'use strict';

const CONFIG = {
  // The conversation id should stay the same unless we share this channel to outside of Flux, which seems impossible
  conversation_id_to_monitor: 'C012HCAMCAV',

};

module.exports = CONFIG;
